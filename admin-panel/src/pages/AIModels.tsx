import React from 'react';

const AIModels: React.FC = () => (
  <div>
    <h2>AI Model Configuration</h2>
    <p>Connect API keys, restrict or whitelist models.</p>
    {/* Model configuration UI */}
  </div>
);

export default AIModels;
