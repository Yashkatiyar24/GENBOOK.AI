import React from 'react';

const Security: React.FC = () => (
  <div>
    <h2>Security Settings</h2>
    <p>Manage two-factor authentication and password recovery.</p>
    {/* Security settings UI */}
  </div>
);

export default Security;
